# ─────────────────────────────────────────────────────────────────────────────
# DLP Proxy System — Python зависимости
# Установка: pip install -r requirements.txt
# Python: 3.10+
# ─────────────────────────────────────────────────────────────────────────────

# ── Proxy & Certificate Management ───────────────────────────────────────────
mitmproxy==10.3.1           # HTTPS MITM прокси с поддержкой аддонов
cryptography==42.0.8        # Генерация CA сертификатов (RSA, x509)

# ── GUI (Client only) ─────────────────────────────────────────────────────────
PyQt6==6.7.0                # GUI фреймворк для клиентского приложения
PyQt6-Qt6==6.7.0
PyQt6-sip==13.6.0

# ── Build — создание .exe (Windows) ──────────────────────────────────────────
pyinstaller==6.6.0          # Сборка в исполняемый файл

# ── Storage — PostgreSQL и S3/MinIO ───────────────────────────────────────────
psycopg[binary]==3.2.9      # PostgreSQL драйвер для хранения бизнес-данных
boto3==1.38.41              # S3 API для хранения capture-файлов и артефактов
